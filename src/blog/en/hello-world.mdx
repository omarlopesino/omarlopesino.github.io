---
title: 'Hello world!'
cid: hello-world
slug: hello-world
description: 'Post to check blog posts are working. It carries one of every element the body can hold, so the theme has something to be judged against.'
language: "en"
pubDate: "2026-06-30"
image:
    src: "/960x540.jpg"
    alt: "Image alt sample"
category: "test"
tags: ["test"]
recommended: ["second-post"]
---

Hello world! This post exists so the post page has something to render, and it holds one of every
element a body can contain.

## A section with a paragraph

Body copy sits at the reading measure and takes its colour from the theme, so it follows the
switcher without a second palette. Inline code such as `drush cr` sits on the recessed surface.

### A sub-heading

Sub-headings are indented in the table of contents rather than numbered, so the numbers count
sections rather than every heading.

> A blockquote takes its rule from the accent colour.

## A section with a list and some code

- The first item.
- The second item, which runs long enough to wrap onto a second line and show what the leading
  looks like when it does.
- The third item.

```php
$node = Node::create(['type' => 'article']);
$node->save();
```

## The last section

Nothing follows this one but the tags, the share button and the author block.
