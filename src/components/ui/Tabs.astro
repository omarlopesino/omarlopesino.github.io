---
interface Props {
    class?: string;
    [key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<div role="tablist" class:list={['tabs tabs-border justify-center', className]} {...rest}>
    <slot />
</div>
